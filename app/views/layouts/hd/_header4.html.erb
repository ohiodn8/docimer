<!-- Static navbar -->
<nav class="navbar navbar-default"><div style="background-color: #008B8B; color: white;">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
        <% if @logotexts.present? %>
            <% @logotexts.each do |logotext| %>
                <%= link_to logotext.text, "/", method: :get, class: "navbar-brand", style: "color: white;" %>
            <% end %>
        <% else %>  
          <%= link_to "The Docimer Project", "/", method: :get, class: "navbar-brand", style: "color: white;" %>
        <% end %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% @pages.each do |page| %>
            <li><%= link_to page.title, page, style: "color: white;", method: :get %></li>
        <% end %>
    <% @sections.each do |section| %>
          <% if section.option.empty? %>
          <% else %>
          <li><%= link_to "Blogs", blogs_path, style: "color: white;", method: :get %></li>
    <% end %><% end %>
          
      </ul>
    </div><!--/.nav-collapse -->
  </div>
    </div>
</nav>
