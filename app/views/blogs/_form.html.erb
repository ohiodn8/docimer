<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul><br>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>      

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :blog_title, class: "form-control" %><br>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :blog_body, class: "form-control", rows: 10 %><br>
  </div>

      <div class="field">
      <%= form.label :category %>: </br>
        <%= form.collection_select :category_id, Category.all, :id , :title, :include_blank => "select One" %>
        &nbsp; &nbsp;<%= link_to 'Add New Category', new_category_path %>
      </div><br>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
